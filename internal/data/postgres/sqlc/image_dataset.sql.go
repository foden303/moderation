// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: image_dataset.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const countImageDatasets = `-- name: CountImageDatasets :one
SELECT COUNT(*) FROM image_datasets
`

func (q *Queries) CountImageDatasets(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countImageDatasets)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countImageDatasetsByCategory = `-- name: CountImageDatasetsByCategory :one
SELECT COUNT(*) FROM image_datasets WHERE category = $1
`

func (q *Queries) CountImageDatasetsByCategory(ctx context.Context, category string) (int64, error) {
	row := q.db.QueryRow(ctx, countImageDatasetsByCategory, category)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createImageDataset = `-- name: CreateImageDataset :one
INSERT INTO image_datasets (
    file_hash, phash, detect_result,
    category, nsfw_score, model_version, source_url, added_by, expires_at
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
ON CONFLICT (file_hash) DO UPDATE SET
    detect_result = EXCLUDED.detect_result,
    category = EXCLUDED.category,
    nsfw_score = EXCLUDED.nsfw_score,
    model_version = EXCLUDED.model_version,
    source_url = EXCLUDED.source_url,
    updated_at = NOW()
RETURNING id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at
`

type CreateImageDatasetParams struct {
	FileHash     string             `json:"file_hash"`
	Phash        int64              `json:"phash"`
	DetectResult []byte             `json:"detect_result"`
	Category     string             `json:"category"`
	NsfwScore    float64            `json:"nsfw_score"`
	ModelVersion string             `json:"model_version"`
	SourceUrl    string             `json:"source_url"`
	AddedBy      string             `json:"added_by"`
	ExpiresAt    pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) CreateImageDataset(ctx context.Context, arg CreateImageDatasetParams) (ImageDataset, error) {
	row := q.db.QueryRow(ctx, createImageDataset,
		arg.FileHash,
		arg.Phash,
		arg.DetectResult,
		arg.Category,
		arg.NsfwScore,
		arg.ModelVersion,
		arg.SourceUrl,
		arg.AddedBy,
		arg.ExpiresAt,
	)
	var i ImageDataset
	err := row.Scan(
		&i.ID,
		&i.FileHash,
		&i.Phash,
		&i.DetectResult,
		&i.Category,
		&i.NsfwScore,
		&i.ModelVersion,
		&i.SourceUrl,
		&i.AddedBy,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteImageDataset = `-- name: DeleteImageDataset :exec
DELETE FROM image_datasets WHERE file_hash = $1
`

func (q *Queries) DeleteImageDataset(ctx context.Context, fileHash string) error {
	_, err := q.db.Exec(ctx, deleteImageDataset, fileHash)
	return err
}

const getAllImageDatasets = `-- name: GetAllImageDatasets :many
SELECT id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at FROM image_datasets
`

func (q *Queries) GetAllImageDatasets(ctx context.Context) ([]ImageDataset, error) {
	rows, err := q.db.Query(ctx, getAllImageDatasets)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ImageDataset
	for rows.Next() {
		var i ImageDataset
		if err := rows.Scan(
			&i.ID,
			&i.FileHash,
			&i.Phash,
			&i.DetectResult,
			&i.Category,
			&i.NsfwScore,
			&i.ModelVersion,
			&i.SourceUrl,
			&i.AddedBy,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getImageDataset = `-- name: GetImageDataset :one
SELECT id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at FROM image_datasets WHERE file_hash = $1
`

func (q *Queries) GetImageDataset(ctx context.Context, fileHash string) (ImageDataset, error) {
	row := q.db.QueryRow(ctx, getImageDataset, fileHash)
	var i ImageDataset
	err := row.Scan(
		&i.ID,
		&i.FileHash,
		&i.Phash,
		&i.DetectResult,
		&i.Category,
		&i.NsfwScore,
		&i.ModelVersion,
		&i.SourceUrl,
		&i.AddedBy,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getImageDatasetByID = `-- name: GetImageDatasetByID :one
SELECT id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at FROM image_datasets WHERE id = $1
`

func (q *Queries) GetImageDatasetByID(ctx context.Context, id int64) (ImageDataset, error) {
	row := q.db.QueryRow(ctx, getImageDatasetByID, id)
	var i ImageDataset
	err := row.Scan(
		&i.ID,
		&i.FileHash,
		&i.Phash,
		&i.DetectResult,
		&i.Category,
		&i.NsfwScore,
		&i.ModelVersion,
		&i.SourceUrl,
		&i.AddedBy,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getImageDatasetByPHash = `-- name: GetImageDatasetByPHash :many
SELECT id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at FROM image_datasets WHERE phash = $1
`

func (q *Queries) GetImageDatasetByPHash(ctx context.Context, phash int64) ([]ImageDataset, error) {
	rows, err := q.db.Query(ctx, getImageDatasetByPHash, phash)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ImageDataset
	for rows.Next() {
		var i ImageDataset
		if err := rows.Scan(
			&i.ID,
			&i.FileHash,
			&i.Phash,
			&i.DetectResult,
			&i.Category,
			&i.NsfwScore,
			&i.ModelVersion,
			&i.SourceUrl,
			&i.AddedBy,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listImageDatasets = `-- name: ListImageDatasets :many
SELECT id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at FROM image_datasets
ORDER BY created_at DESC
LIMIT $1 OFFSET $2
`

type ListImageDatasetsParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

func (q *Queries) ListImageDatasets(ctx context.Context, arg ListImageDatasetsParams) ([]ImageDataset, error) {
	rows, err := q.db.Query(ctx, listImageDatasets, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ImageDataset
	for rows.Next() {
		var i ImageDataset
		if err := rows.Scan(
			&i.ID,
			&i.FileHash,
			&i.Phash,
			&i.DetectResult,
			&i.Category,
			&i.NsfwScore,
			&i.ModelVersion,
			&i.SourceUrl,
			&i.AddedBy,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listImageDatasetsByCategory = `-- name: ListImageDatasetsByCategory :many
SELECT id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at FROM image_datasets
WHERE category = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type ListImageDatasetsByCategoryParams struct {
	Category string `json:"category"`
	Limit    int32  `json:"limit"`
	Offset   int32  `json:"offset"`
}

func (q *Queries) ListImageDatasetsByCategory(ctx context.Context, arg ListImageDatasetsByCategoryParams) ([]ImageDataset, error) {
	rows, err := q.db.Query(ctx, listImageDatasetsByCategory, arg.Category, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ImageDataset
	for rows.Next() {
		var i ImageDataset
		if err := rows.Scan(
			&i.ID,
			&i.FileHash,
			&i.Phash,
			&i.DetectResult,
			&i.Category,
			&i.NsfwScore,
			&i.ModelVersion,
			&i.SourceUrl,
			&i.AddedBy,
			&i.ExpiresAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateImageDataset = `-- name: UpdateImageDataset :one
UPDATE image_datasets
SET category = $2, nsfw_score = $3, source_url = $4, added_by = $5, updated_at = NOW()
WHERE id = $1
RETURNING id, file_hash, phash, detect_result, category, nsfw_score, model_version, source_url, added_by, expires_at, created_at, updated_at
`

type UpdateImageDatasetParams struct {
	ID        int64   `json:"id"`
	Category  string  `json:"category"`
	NsfwScore float64 `json:"nsfw_score"`
	SourceUrl string  `json:"source_url"`
	AddedBy   string  `json:"added_by"`
}

func (q *Queries) UpdateImageDataset(ctx context.Context, arg UpdateImageDatasetParams) (ImageDataset, error) {
	row := q.db.QueryRow(ctx, updateImageDataset,
		arg.ID,
		arg.Category,
		arg.NsfwScore,
		arg.SourceUrl,
		arg.AddedBy,
	)
	var i ImageDataset
	err := row.Scan(
		&i.ID,
		&i.FileHash,
		&i.Phash,
		&i.DetectResult,
		&i.Category,
		&i.NsfwScore,
		&i.ModelVersion,
		&i.SourceUrl,
		&i.AddedBy,
		&i.ExpiresAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
